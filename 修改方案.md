采用以下分工：

1.  **qwen3.7-max 模型来执行。**
    *   用它扮演提议者、求解者、验证者，以及高质量负样本求解者。

2.  **让你的 qwen3-8b 模型只扮演一个角色：生成“无引导”的原始答案。**
    *   对8b模型**不使用** `negative_solver_system_prompt` 里所有“刻意犯错”的指令。
    *   给它一个最朴素的任务：**“请分析以下财务问题：{question}”**
    *   让它用最自然的水平去推理。它做对了就是正样本，做错了就是最真实的负样本。这些负样本直接暴露了它真实的推理弱点，对训练 PRM 价值最高。
```text
同一个问题
    │
    ├──→ 235B 正样本求解者 ──→ 【标准答案】 ──────────────┐
    │     (solver_system_prompt)                           │
    │                                                      │
    ├──→ 235B 负样本求解者 ──→ 【待验证负样本】 ──→ 负样本验证者 ──→ 有效/无效
    │     (negative_solver)           │           (对比标准答案)
    │                                 │
    └──→ 8B 策略模型 ──→ 【待标注样本】 ──→ 对比标准答案 ──→ 标注为正/负样本
          (采样Prompt)
```
## 角色分工总览

| 角色 | 扮演者 | Prompt | 核心任务 | 产出 |
|------|--------|--------|----------|------|
| **提议者** | qwen3.7-max | `proposer_system_prompt` | 基于财务数据设计分析问题 | 高质量问题（难度递增） |
| **正样本求解者** | qwen3.7-max | `solver_system_prompt` | 生成正确的分步推理 | 标准参考答案（正样本） |
| **负样本求解者** | qwen3.7-max | `negative_solver_system_prompt` | 刻意注入专业错误的推理 | 有迷惑性的错误答案（负样本） |
| **正样本验证者** | qwen3.7-max | `validator_system_prompt` | 验证正样本答案的正确性 | 质量过滤后的正样本 |
| **负样本验证者** | qwen3.7-max | `negative_validator_system_prompt` | 验证负样本错误质量 | 质量过滤后的负样本 |
| **策略模型采样** | **qwen3-8b** | **纯推理 Prompt**（无错误注入指令） | 自然推理，对错随缘 | 真实的正确/错误推理样本 |
| **策略样本验证者** | qwen3.7-max | `8B_validator_system_prompt` | 策略模型采样质量 | 质量过滤后的正/负样本 |

## 两种模型在流程中的位置

| 流程阶段 | 执行者 | 输入 | 输出 | 用途 |
|----------|--------|------|------|------|
| 1. 问题生成+参考答案 | qwen3.7-max 提议者 | 财务数据 + 任务类型 | 分析问题 | 所有样本共用，标准答案 |
| 2a. 正样本生成 | qwen3.7-max 正样本求解者 | 问题 | 正确推理过程 | PRM 正例训练 |
| 2b. 负样本生成 | qwen3.7-max 负样本求解者 | 问题 | 含错误的推理过程 | PRM 负例训练 |
| 2c. 自然采样 | **qwen3-8b 策略模型** | 问题（无引导） | 自然水平的推理 | **PRM 核心训练数据** |
| 3a. 正样本校验 | qwen3.7-max 验证者 | qwen3.7-max/qwen3-8b答案 vs 标准 | 通过/不通过 | 保证数据质量 |
| 3b. 负样本校验 | qwen3.7-max 验证者 | qwen3.7-max/qwen3-8b答案 vs 标准 | 通过/不通过 | 保证错误质量 |

## 最终训练数据构成
> 1个问题对应1个标准答案（和问题一起生成），1个正样本，1个负样本，3个策略模型样本

| 数据来源 | 扮演者 | 样本类型 | 占比建议 | 价值定位 |
|----------|--------|----------|----------|----------|
| qwen3.7-max 生成的正样本 | 求解者 | 正确推理 | 20% | 教科书级范例 |
| qwen3.7-max 生成的负样本 | 负样本求解者 | 含迷惑性错误 | 20% | 高质量反例 |
| **qwen3-8b 策略模型自然采样** | **策略模型** | 自然正确/错误 | **60%** | **最真实的训练数据** |

## Prompt 使用对照

| Prompt 文件中的类 | 使用模型 | 是否保留 |
|-------------------|----------|----------|
| `proposer_system_prompt` | qwen3.7-max | ✅ 保留 |
| `solver_system_prompt` | qwen3.7-max | ✅ 保留 |
| `negative_solver_system_prompt` | qwen3.7-max | ✅ 保留（含7类错误注入） |
| `validator_system_prompt` | qwen3.7-max | ✅ 保留 |
| `negative_validator_system_prompt` | qwen3.7-max | ✅ 保留 |
| **（需新增）朴素推理 Prompt** | **qwen3-8b** | 🆕 仅要求推理，无引导 |

## qwen3-8b 模型需要的新 Prompt（建议）

```python
# qwen3-8b 策略模型采样专用 Prompt
sampling_system_prompt: str = Field(
    default="""你是一位金融分析师，请基于提供的财务数据，对给定问题进行分步推理分析。

问题：{question}

请展示你的完整推理过程，包括：
- 分析步骤和逻辑
- 使用的财务数据
- 最终结论

【必须】严格按照以下 JSON 格式返回：
```json
{
  "分析过程": {
    "步骤1": "第一步推理分析",
    "步骤2": "第二步推理分析", 
    "步骤3": "第三步推理分析"
  },
  "分析结论": "你的最终结论"
}
```""",
    description="qwen3-8b策略模型采样Prompt"
)
```

**核心差异**：qwen3-8b 的 Prompt 去掉了所有"错误注入"、"专业要求"等引导，让它以最自然的状态推理，这样才能暴露它的真实弱点和错误模式。

---

| 参数 | 作用 | 高值效果 | 低值效果 |
|------|------|----------|----------|
| **Temperature** | 控制随机性 | 多样性强，适合需要创造力的任务 | 确定性高，适合需要准确性的任务 |
| **Max Tokens** | 控制生成长度上限 | 允许长输出 | 限制输出，防止冗余 |

## 按角色推荐设置

### qwen3.7-max（数据生成主力）

| 角色 | Temperature | Max Tokens | 理由 |
|------|-------------|------------|------|
| **提议者**（问题生成） | 0.7 - 0.9 | 2048 | 需要多样性的问题，避免重复 |
| **正样本求解者**（标准答案） | 0.1 - 0.3 | 4096 | 需要准确、稳定的标准答案 |
| **负样本求解者**（错误注入） | 0.6 - 0.8 | 4096 | 需要多样化的错误类型和方式 |
| **正样本验证者** | 0.0 - 0.1 | 1024 | 评判任务需要高度一致 |
| **负样本验证者** | 0.0 - 0.1 | 1024 | 评判任务需要高度一致 |
| **8B采样标注者** | 0.0 - 0.1 | 1024 | 评判任务需要高度一致 |

### qwen3-8b（策略模型采样）

| 角色 | Temperature | Max Tokens | 理由 |
|------|-------------|------------|------|
| **策略模型采样** | 0.6 - 0.8 | 4096 | 模拟真实推理时的自然多样性 |